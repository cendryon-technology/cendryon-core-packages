<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <PropertyGroup> 
        <PackageId>Cendryon.Mapping</PackageId>
        <Version>1.0.0</Version>
        <Authors>Cendryon s.r.o.</Authors>
        <Company>Cendryon s.r.o.</Company>

        <!-- Descriptive metadata -->
        <Title>Cendryon.Mapping</Title>
        <Description>
          Lightweight mapping abstraction that integrates cleanly with Cendryon.Results.
          Supports synchronous and asynchronous transformations, plus Result&lt;T&gt; → Result&lt;U&gt; conversions.
          Optionally integrates with Mapster or AutoMapper.
        </Description>
        <PackageTags>Cendryon;results;error;problem-details;cqrs;clean-architecture;azure;azure-functions</PackageTags>
        <RepositoryUrl>https://github.com/cendryon-technology/cendryon-core-packages</RepositoryUrl>
        <PackageProjectUrl>https://github.com/cendryon-technology/cendryon-core-packages</PackageProjectUrl>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageLicenseFile>LICENSE</PackageLicenseFile>
        <PackageIcon>icon.png</PackageIcon>
        <RepositoryType>git</RepositoryType>

        <!-- Optional packaging controls -->
        <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
        <IncludeSymbols>false</IncludeSymbols>
        <IncludeSource>false</IncludeSource>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\Cendryon.Results\Cendryon.Results.csproj" />
    </ItemGroup>

    <ItemGroup>
        <None Include="../../README.md" Pack="true" PackagePath="" />
        <None Include="../../LICENSE" Pack="true" PackagePath="" />
        <None Include="../../icon.png" Pack="true" PackagePath="" />
    </ItemGroup>

</Project>
